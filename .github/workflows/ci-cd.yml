name: CI-CD

on:
  push:
    branches: [development, testing, main]
  pull_request:
    branches: [development, testing, main]

jobs:
  dev-check:
    name: Development - Compile/Checks
    if: github.ref == 'refs/heads/development'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Validate
        run: mvn -B -q validate

      - name: Compile (syntax/compile issues)
        run: mvn -B -q clean compile

  test-run:
    name: Testing - Tests (safe when none yet)
    if: github.ref == 'refs/heads/testing'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      # This will NOT fail if no tests exist, but will run them once you add tests later.
      - name: Run tests (skip failure if none yet)
        run: mvn -B test -DfailIfNoTests=false

  main-full:
    name: Main - Full Build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Full verification
        run: mvn -B clean verify -DfailIfNoTests=false

      - name: Package
        run: mvn -B package -DfailIfNoTests=false
